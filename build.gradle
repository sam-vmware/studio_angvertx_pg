allprojects {
    apply plugin: 'idea'
    sourceCompatibility = '1.7'
    targetCompatibility = '1.7'
    group = 'com.vmware.studio'
    version = '1.0'
}

subprojects {
    ext.GLOBAL_CONF = [
        vertx: [
            libDir: "/home/samueldoyle/Projects/GitHub/Vertx/vertx/build/current/lib"
        ]
    ]
    repositories {
        mavenCentral()
    }
    dependencies {
        //compile fileTree(dir: "/home/samueldoyle/Projects/GitHub/Vertx/vertx/build/current/lib", includes: ['*.jar'])
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}
